@model List<Biblioteca.Models.Libro.Idioma>
@{
    ViewData["Title"] = "Listar";
}
<div class="account-listar">
    <h2 class="text-center mb-4" style="font-family:OMEGLE ; font-size: 45px"><b>Lista de Idiomas</b></h2>
<ol class="breadcrumb mb-4">
    <li class="breadcrumb-item">
        <form action="@Url.Action("Create", "Idioma")" method="get" style="display:inline;">
            <button type="submit" class="btn btn-primary">Nuevo Idioma</button>
        </form>
    </li>
</ol>

<div class="card mb-4">
    <div class="card-header">
        <i class="fas fa-table me-1"></i>
        Listado
    </div>
    <div class="card-body">
        <table id="datatablesSimple">
            <thead>
                <tr>
                    <th>Idioma</th>
                    <th>Operaciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var idioma in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => idioma.IdiomaLibro)</td>
                        <td>
                            <a href="@Url.Action("Edit", "Idioma", new { id = idioma.Id })" class="btn btn-primary me-2"><i class="fas fa-edit"></i></a>
                            <form action="@Url.Action("Delete", "Idioma", new { id = idioma.Id })" method="post" style="display:inline;">
                                @Html.AntiForgeryToken() <!-- Agregar token de anti-falsificación -->
                                <button type="submit" class="btn btn-danger me-2" onclick="return confirm('¿Estás seguro de que deseas eliminar el IDIOMA seleccionado?');"><i class="fas fa-trash"></i></button>
                            </form>
                        </td>
                    </tr>

                }

            </tbody>
        </table>
    </div>
</div>
</div>

